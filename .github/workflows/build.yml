name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Fix Directory Permissions
      run: sudo chmod -R 777 $GITHUB_WORKSPACE

    # Fixed: Added '-w /home/user/app' so Docker enters the project directory where buildozer.spec lives
    - name: Build APK with Docker Container
      run: |
        yes | docker run -i --rm \
          -v ${{ github.workspace }}:/home/user/app \
          -w /home/user/app \
          kivy/buildozer android debug

    - name: Fix Artifact Ownership Permissions
      if: always()
      run: sudo chown -R $USER:$USER .

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: mikeybot-apk
        path: bin/*.apk
